## prometheus 监控使用
management:
  endpoints:
    web:
      exposure:
        include: 'prometheus' #[*]
  metrics:
    tags:
      application: ${spring.application.name}
  endpoint:
    health:
      show-details: always
